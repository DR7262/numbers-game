export { todaysValues };

let target = 200

let numbers = [10, 25, 7, 50, 2, 8]

const privateData = new WeakMap();

const todaysValues = {
    target: target,
    numbers: numbers,
    privateData: privateData
}